describe('Logs into dashbaord', () => {
  it('Loads', () => {
    cy.visit('/')

    expect(true).to.equal(true)
  })

  it('Renders log in', () => {
    cy.visit('/')

    expect(true).to.equal(true)
  })

  it('Can log in', () => {
    cy.visit('/')

    expect(true).to.equal(true)
  })

  it('Redirects to dashbaord', () => {
    cy.visit('/')

    expect(true).to.equal(true)
  })
})
